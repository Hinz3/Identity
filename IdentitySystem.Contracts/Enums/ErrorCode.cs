namespace IdentitySystem.Contracts.Enums
{
    public enum ErrorCode
    {
        INVALID_REQUEST = 0,

        INVALID_CLIENT_ID = 1,
        INVALID_SCOPE = 2,
        INVALID_REDIRECT_URL = 3,
        INVALID_CREDENTIALS = 4,
        INVALID_GRANT_TYPE = 5,
    }
}
